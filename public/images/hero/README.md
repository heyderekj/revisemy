# Hero product shots

Drop full-quality exports here (PNG preferred). The homepage picks the first match:

| File | Used when |
|------|-----------|
| `desktop.png` (or `.webp` / `.jpg`) | Viewports ≥ 640px |
| `mobile.png` (or `.webp` / `.jpg`) | Viewports &lt; 640px |

Current placeholders are low-res JPGs from a chat export — replace them with your HQ captures.
